#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a[] = {0,1,2,3,4,5};
	int *p, *q;
	p = &a[2]
	printf("elemento apunta p %d\n", *p)
	q = p+3;
	printf("elemento apunta q %d\n", *q)
	system("PAUSE");
	
	return 0;
}
